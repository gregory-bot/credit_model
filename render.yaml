services:
  - type: web
    name: credit-scoring-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn credit_scoring.api:app --host 0.0.0.0 --port 
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
